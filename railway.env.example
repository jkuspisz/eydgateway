# Railway Deployment Configuration
# This file contains environment variables and deployment settings for Railway

# Build Configuration
RAILWAY_DOCKERFILE_PATH=Dockerfile

# Runtime Environment
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:$PORT

# Database will be automatically configured by Railway's PostgreSQL service
# The DATABASE_URL environment variable will be provided by Railway

# Optional: Custom Port (Railway will set this automatically)
# PORT=8080

# Security Settings (set these in Railway dashboard)
# ASPNETCORE_FORWARDEDHEADERS_ENABLED=true
